# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on PR
on: pull_request
permissions:
  checks: write
  contents: read
  pull-requests: write
jobs:
  build_and_preview:
    if: ${{ github.event.pull_request.head.repo.full_name == github.repository }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up .NET
        uses: actions/setup-dotnet@v2
        with:
          dotnet-version: '8.0.x'
      - name: Publish .NET project
        run: dotnet publish -c Release -o bin/Release/net8.0/publish
      - name: List directory contents
        run: ls -la bin/Release/net8.0/publish/wwwroot
      - name: Deploy to Firebase Hosting
        run: |
          if [ -d "bin/Release/net8.0/publish/wwwroot" ]; then
            npx firebase-tools@latest hosting:channel:deploy pr${{ github.event.number }}-UpdateConfig --expires 7d --project tracker-c119a --json
          else
            echo "Error: Directory 'bin/Release/net8.0/publish/wwwroot' for Hosting does not exist."
            exit 1
          fi
        env:
          FIREBASE_TOKEN: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_TRACKER_C119A }}